const nomeEstudantes = ['João', 'Juliana', 'Caio', 'Ana'];
const notas = [10, 8, 7.5, 9];

const lista = [nomeEstudantes,notas];

console.log(
    `a aluna da posição 1 da lista é: ${lista[0][1]}.
    a nota desta aluna é ${lista[1][1]}`
);
